extends layouts/main.pug
include mixins/videoBlock

block content
    .videos
        each item in video_db
            +videoBlock({
                id: item.id,
                title: item.title,
                views: item.views,
                videoFile: item.src
            })
        